import React from 'react';
import { SettingsContainer } from './Settings.styles';

const Settings: React.FC = () => {
    return (
        <SettingsContainer>
            <h1>Settings</h1>
            <p>Manage application preferences here.</p>
        </SettingsContainer>
    );
};

export default Settings;
